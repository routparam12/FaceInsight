# --- Web framework ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- Config / validation ---
pydantic>=2.9
pydantic-settings>=2.5

# --- Persistence ---
SQLAlchemy>=2.0.35
alembic>=1.13
aiosqlite>=0.20          # dev / test driver
asyncpg>=0.29            # production Postgres driver
pgvector>=0.3            # prod-only: vector column + ANN index for 1:N

# --- Security ---
PyJWT>=2.9
cryptography>=43         # Fernet encryption of enrollment source images

# --- Face math ---
numpy>=2.0

# --- Timezones (Windows has no system zoneinfo db) ---
tzdata>=2024.1

# --- Optional: server-side embedding generation for enrollment ---
# onnxruntime is imported lazily; install it only where enrollment runs.
# onnxruntime>=1.19 ; python_version < "3.14"

# --- Tests ---
pytest>=8.3
pytest-asyncio>=0.24
httpx>=0.27
